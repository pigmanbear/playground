import { Sink, Scheduler, Disposable } from 'most';
import { NodeStream } from './types';
export declare function addListeners(stream: NodeStream, endEventName: string, dataEventName: string, sink: Sink<any>, scheduler: Scheduler): Disposable<any>;
