"use strict";
var FromStreamDisposable = (function () {
    function FromStreamDisposable(source, sink) {
        this.source = source;
        this.sink = sink;
        this.disposed = false;
    }
    FromStreamDisposable.prototype.dispose = function () {
        if (this.disposed)
            return;
        this.disposed = true;
        var remaining = this.source.remove(this.sink);
        return remaining === 0 && this.source._dispose();
    };
    return FromStreamDisposable;
}());
exports.FromStreamDisposable = FromStreamDisposable;
//# sourceMappingURL=FromStreamDisposable.js.map