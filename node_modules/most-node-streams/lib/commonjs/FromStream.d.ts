import { Sink, Scheduler } from 'most';
import { MulticastSource } from '@most/multicast';
import { FromStreamDisposable } from './FromStreamDisposable';
import { NodeStream } from './types';
export declare class FromStream<T> extends MulticastSource<T> {
    private stream;
    private endEventName;
    private dataEventName;
    private _disposable;
    constructor(stream: NodeStream, endEventName: string, dataEventName: string);
    run(sink: Sink<T>, scheduler: Scheduler): FromStreamDisposable;
}
