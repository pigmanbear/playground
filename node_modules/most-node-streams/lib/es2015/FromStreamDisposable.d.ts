import { Sink, Disposable } from 'most';
import { FromStream } from './FromStream';
export declare class FromStreamDisposable implements Disposable<any> {
    private source;
    private sink;
    private disposed;
    constructor(source: FromStream<any>, sink: Sink<any>);
    dispose(): any;
}
